# ── Streamlit UI ──────────────────────────────────────────────────
streamlit>=1.35.0

# ── FashionCLIP / CLIP model loading ─────────────────────────────
# transformers loads patrickjohncyh/fashion-clip from HuggingFace
transformers>=4.40.0
# torch: install the correct variant for your machine:
#   CPU only  → pip install torch torchvision
#   CUDA 11.8 → pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1 → pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# ── HuggingFace model hub ────────────────────────────────────────
huggingface-hub>=0.23.0
safetensors>=0.4.0

# ── Vector database ──────────────────────────────────────────────
chromadb>=0.5.0

# ── Image processing ─────────────────────────────────────────────
Pillow>=10.0.0

# ── Background removal ───────────────────────────────────────────
# Downloads U2Net model (~170MB) on first run to ~/.u2net/
rembg>=2.0.50
# rembg requires onnxruntime — install CPU or GPU variant:
#   CPU  → onnxruntime
#   GPU  → onnxruntime-gpu  (and uninstall onnxruntime first)
onnxruntime>=1.17.0

# ── Face detection (for preprocessing / similarity improvement) ───
# Used in build_feature_store.py preprocessing pipeline
opencv-python-headless>=4.9.0

# ── Data processing ──────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
openpyxl>=3.1.0          # required by pandas to read .xlsx files

# ── HTTP requests (Gemini + OpenRouter API calls) ─────────────────
requests>=2.31.0

# ── Environment variable loading ──────────────────────────────────
python-dotenv>=1.0.0

# ── Tokenizer dependency for transformers ────────────────────────
tokenizers>=0.19.0